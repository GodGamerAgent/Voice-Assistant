package com.example.data.model

enum class AssistMode(val title: String, val description: String) {
    WHISPER("Whisper Mode", "Voice dictation with AI cleanup"),
    REPLY("Reply Mode", "Context-aware AI smart reply")
}
